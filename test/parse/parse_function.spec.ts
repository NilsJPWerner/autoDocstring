import { expect } from 'chai';
import 'mocha';

import { getDefinition } from 'parse/parse_function';

describe('tokenizeDefinition', () => {

    // it("Should", () => {
    //     var document = vscode.workspace.openTextDocument({language: 'text'}).then( doc => {
    //         doc.
    //     })

    //     )
    //     document.fileName

    //     var result = getDefinition(document, );

    //     expect(result).to.have.ordered.members([
    //         'argument',
    //         'kwarg=\"abc\"'
    //     ]);
    // });

});
