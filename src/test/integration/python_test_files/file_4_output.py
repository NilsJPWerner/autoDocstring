from typing import Callable, List, Enum

def build(self,
            my_id: uuid.UUID,
            my_callable: Callable[[int, str, List[object], Enum],
                                        dict] = None) -> None:
    """_summary_

    :param my_id: _description_
    :type my_id: uuid.UUID
    :param my_callable: _description_, defaults to None
    :type my_callable: Callable[[int, str, List[object], Enum], dict], optional
    """
    pass
